---
import Calculadora from "@/components/Calculadora.astro";
import ContainerSection from "@/components/ui/ContainerSection.astro";
import DivMejoraNivel from "@/components/sections/page-beneficios/page-referidos/DivMejoraNivel.astro"; // ajusta la ruta según tu estructura
---

<ContainerSection
  ariaLabelledby="titulo-hero-referidos"
  bgColor="bg-linear-to-b from-primary-500 via-primary-700 to-primary-900"
  textColor="text-white"
  class="relative overflow-hidden"
  maxWidth="max-w-350"
  classContainer="relative z-10"
>
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-center">
    <!-- Copy principal - 5 columnas -->
    <header class="lg:col-span-5 space-y-6 lg:space-y-8 text-center lg:text-left">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/10 border border-white/20">
        <svg class="size-5 text-secondary-200" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
          <path d="M2 10.5a1.5 1.5 0 113 0v6a1.5 1.5 0 01-3 0v-6zM6 10.333v5.43a2 2 0 001.106 1.79l.05.025A4 4 0 008.943 18h5.416a2 2 0 001.962-1.608l1.2-6A2 2 0 0015.56 8H12V4a2 2 0 00-2-2 1 1 0 00-1 1v.667a4 4 0 01-.8 2.4L6.8 7.933a4 4 0 00-.8 2.4z" />
        </svg>
        <span class="text-sm font-semibold">Programa de Referidos</span>
      </div>

      <h1
        id="titulo-hero-referidos"
        class="text-3xl lg:text-5xl xl:text-6xl font-bold tracking-tight"
      >
        Invita, opera y
        <span class="mt-2 bg-linear-to-r from-secondary-200 via-secondary-300 to-secondary-400 bg-clip-text text-transparent">
          gana más pips
        </span>
        en cada cambio
      </h1>

      <p class="text-base text-primary-50/90 leading-relaxed max-w-xl mx-auto lg:mx-0">
        Comparte Market Dollar con tus contactos y <strong class="text-white">accede a mejores pips</strong> en tus
        operaciones conforme más personas se unan a través de ti.
      </p>

      <div
        class="
          inline-flex items-center gap-3
          px-5 py-3
          rounded-2xl
          bg-linear-to-r from-secondary-300/90 to-secondary-400/90
          border border-secondary-300/50
          text-neutral-900
          text-sm lg:text-base font-semibold
          shadow-lg shadow-secondary-400/20
        "
      >
        <span class="relative flex h-2.5 w-2.5" aria-hidden="true">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-white opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-white"></span>
        </span>
        Más referidos = Mejores pips para ti
      </div>

      <div class="flex flex-wrap items-center justify-center lg:justify-start gap-4 text-sm text-primary-100/90">
        <span class="flex items-center gap-2">
          <svg class="size-5 text-success-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
          Ganancias inmediatas
        </span>
        <span class="hidden sm:inline text-primary-300/50">•</span>
        <span class="flex items-center gap-2">
          <svg class="size-5 text-success-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"
            />
          </svg>
          Sin límite de referidos
        </span>
      </div>
    </header>

    <!-- Card de niveles - 4 columnas -->
    <div class="lg:col-span-4">
      <DivMejoraNivel />
    </div>

    <!-- Conversor - 3 columnas -->
    <div class="lg:col-span-3 mx-auto">
      <Calculadora />
    </div>
  </div>
</ContainerSection>

<style>
  @keyframes ping {
    75%,
    100% {
      transform: scale(2);
      opacity: 0;
    }
  }

  .animate-ping {
    animation: ping 1.5s cubic-bezier(0, 0, 0.2, 1) infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-ping {
      animation: none;
    }
  }
</style>
