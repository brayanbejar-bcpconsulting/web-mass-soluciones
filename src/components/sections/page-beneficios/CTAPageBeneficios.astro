---
import ContainerSection from "@/components/ui/ContainerSection.astro";
import SectionHeading from "@/components/ui/SectionHeading.astro";
import Icon from "@/components/ui/Icon.astro";
import Button from "@components/ui/Button.astro";
import { APP_URLS } from "@/utils/navigation";
---

<ContainerSection
  id="cta-final"
  ariaLabelledby="cta-final-titulo"
  class="relative overflow-hidden"
  maxWidth="max-w-6xl"
  classContainer="flex flex-col gap-8"
>

  <article class="relative z-10 overflow-hidden rounded-3xl lg:rounded-[2.5rem] bg-linear-to-br from-primary-600 via-primary-700 to-primary-800 shadow-2xl px-6 sm:px-10 lg:px-16 py-12 lg:py-20 text-white text-center flex flex-col items-center">
    
    <div class="absolute -z-1 top-0 right-0 size-64 bg-secondary-400/15 rounded-full lg:blur-3xl translate-x-1/3 -translate-y-1/3 pointer-events-none" aria-hidden="true"></div>
    <div class="absolute -z-1 bottom-0 left-0 size-80 bg-primary-400/15 rounded-full lg:blur-3xl -translate-x-1/3 translate-y-1/3 pointer-events-none" aria-hidden="true"></div>

    <header class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/10 border border-white/20 mb-8">
      <span class="relative flex size-3">
        <span class="animate-ping absolute inset-0 rounded-full bg-secondary-400 opacity-75"></span>
        <span class="relative size-3 rounded-full bg-secondary-400"></span>
      </span>
      <span class="text-sm font-semibold">¡Oferta especial por tiempo limitado!</span>
    </header>

    <SectionHeading 
      id="cta-final-titulo"
      align="center"
      title="Empieza a cambiar dólares con una mejor tasa hoy"
      description="Regístrate ahora y accede a una tasa preferencial desde tu primera operación. **Rápido, seguro y 100% online.**"
      barColor="bg-secondary-400"
      textColor="text-primary-50/90"
      class="mb-10 max-w-3xl"
    />

    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 lg:gap-6 w-full pt-4 max-w-4xl">
      {[
        { icon: 'check-circle', text: 'Registro en 2 minutos' },
        { icon: 'shield-check', text: '100% seguro y confiable' },
        { icon: 'currency-dollar', text: 'Transferencias rápidas' }
      ].map((item) => (
        <article class="flex items-center gap-3 justify-center sm:justify-start bg-white/5 rounded-2xl p-4 border border-white/10">
          <div class="shrink-0 size-10 rounded-full bg-success-500/20 flex items-center justify-center">
            <Icon name={item.icon} class="size-6 text-success-400" />
          </div>
          <span class="text-sm font-semibold">{item.text}</span>
        </article>
      ))}
    </div>

    <footer class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-12 w-full max-w-4xl">
      <Button variant="secondary" size="lg" href={APP_URLS.register} class="w-full sm:w-auto group">
        <Icon name="user" class="group-hover:rotate-12 transition-transform" />
        Crear cuenta
        <Icon name="arrow-right" class="group-hover:translate-x-1 transition-transform" />
      </Button>
      <Button variant="outline" size="lg" href={APP_URLS.login} class="w-full sm:w-auto">
        <Icon name="login" />
        Ya tengo cuenta
      </Button>
    </footer>

    <footer class="pt-10 w-full max-w-4xl border-t border-white/10 mt-10 space-y-6">
      <div class="flex flex-wrap items-center justify-center gap-x-6 gap-y-3 text-sm text-primary-100/80">
        <span class="flex items-center gap-2"><Icon name="shield-check" class="size-4 text-success-400" /> Plataforma segura</span>
        <span class="flex items-center gap-2"><Icon name="currency-dollar" class="size-4 text-success-400" /> Sin costos ocultos</span>
      </div>

      <div class="flex items-center justify-center gap-3">
        <div class="flex -space-x-2">
          {[1, 2, 3, 4].map((i) => (
            <div class="size-8 rounded-full bg-primary-500 border-2 border-primary-700 flex items-center justify-center text-[10px] font-bold overflow-hidden">
              {i === 4 ? '+1.5k' : <Icon name="user" class="size-3 text-white/50" />}
            </div>
          ))}
        </div>
        <span class="text-sm text-primary-100/90 font-medium"> +1,500 usuarios confían en nosotros </span>
      </div>
    </footer>
  </article>

  <aside class="max-w-2xl mx-auto bg-white rounded-2xl shadow-xl p-6 border border-neutral-200 flex items-start gap-4">
    <div class="shrink-0 size-12 rounded-full bg-linear-to-br from-primary-500 to-primary-600 flex items-center justify-center text-white font-bold text-lg">
      M
    </div>
    <div class="space-y-2">
      <div class="flex items-center gap-1" aria-label="5 estrellas">
        {[1, 2, 3, 4, 5].map(() => <Icon name="star" class="size-4 text-secondary-400 fill-secondary-400" />)}
      </div>
      <blockquote class="text-neutral-700 text-sm italic">
        "La mejor plataforma para cambiar dólares. Rápido, seguro y con excelente atención al cliente."
      </blockquote>
      <cite class="block text-xs text-neutral-500 font-bold uppercase tracking-wider not-italic">
        María G. • Cliente verificada
      </cite>
    </div>
  </aside>
</ContainerSection>